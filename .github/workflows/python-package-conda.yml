name: Daily 13D/13G Monitor

on:
  schedule:
    - cron: "0 9 * * *"   # Runs every day at 09:00 UTC
  workflow_dispatch:       # allows you to run manually

jobs:
  run-monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install requests

      - name: Run daily monitor
        env:
          SEC_API_KEY: ${{ 76d9e4b3bcba4e015a915503532bd98a9dd39706da64c424d067be43d472647f }}
          TELEGRAM_BOT_TOKEN: ${{ 8547260576:AAG5GHEWGSoP3M7J2_69kmrpd0OLQhm6dhI }}
          TELEGRAM_CHAT_ID: ${{ 5937443021 }}
        run: python 13G.py
